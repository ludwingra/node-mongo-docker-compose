# Docker compose - project: node mongodb by mongoose

### npm install
### npm run dev

### Create images and execution for the docker
```
  docker-compose build
  docker-compose up
```
### URL where it is running
http://localhost:5000/